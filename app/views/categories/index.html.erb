<div class="container">
  <h2>Listing of Categories</h2>
  <h2>Search Restaurants by Category: </h2>
  <ul id="categories">
    <% Category.all.each do |category| %>
      <li>
        <%= link_to category.name, category_path(category) %>
      </li>
    <% end %>
  </ul>
</div>